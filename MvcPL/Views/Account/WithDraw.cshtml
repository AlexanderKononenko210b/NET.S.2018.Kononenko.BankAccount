@model IEnumerable<string>
@{
    ViewBag.Title = "WithDraw";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>WithDraw account</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>WithDraw information</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.Label("Number account", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("numberAccount", new SelectList(Model), new { htmlAttributes = new { @class = "form-control" } })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("WithDraw value", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.Editor("withdraw", new { htmlAttributes = new { @class = "form-control" } })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "GetAll")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

